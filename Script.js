function initializeTeams() {
    const teams = [
        { name: document.getElementById('team1').value, points: 0, goalDifference: 0 },
        { name: document.getElementById('team2').value, points: 0, goalDifference: 0 },
        { name: document.getElementById('team3').value, points: 0, goalDifference: 0 },
        { name: document.getElementById('team4').value, points: 0, goalDifference: 0 }
    ];
    // Generate matches and display them...
    // This is just a start. You'll need to add functionality to input match results,
    // calculate points, display the bracket, and so on.
}
